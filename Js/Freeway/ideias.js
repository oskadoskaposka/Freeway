
// função para mover todas as imagens, nao está funcionando descobrir o porque
let mover = (banana, y) => {
    if(banana < -50) {banana +=750}
    else {banana -=y}
  }

  // criar mais figuras
  